# Copy to .env and adjust. The example server loads .env at startup.
DATABASE_URL=postgres://localhost/architect
# Optional: plugin directory containing manifest.json + schemas.json, tables.json, etc. If unset, config is loaded from DB (insert via config APIs or POST /api/v1/config/plugin).
# PLUGIN_PATH=sample
# RUST_LOG=architect_sdk=info
